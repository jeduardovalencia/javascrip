/*7. Escriba una función la cual reciba como parámetro una cadena y genere como
retorno la misma cadena pero con todos su contenido en mayúscula */


let parametro = prompt("escribe el parametro")
let leer = parametro.length


function string(resultado) {
    for (let i = 0; i<= leer ; i++) {
        resultado = toUpperCase
    
        
    }return resultado
}
alert (`su string en mayuscula es ${resultado}`)
